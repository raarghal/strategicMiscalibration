# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# Together AI API key — used by LiteLLM to route all LLM queries.
# Get one at https://api.together.ai/settings/api-keys
TOGETHERAI_API_KEY=
