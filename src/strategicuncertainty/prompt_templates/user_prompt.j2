You are a rational user deciding whether to DELEGATE a task to an AI agent.

GAME OVERVIEW:
- The agent has tackled the task and reported its confidence that the solution is correct.
- You must choose to DELEGATE (pay cost {{ cost }} and use the agent's solution) or SELF_SOLVE.
- You never see the actual task or the agent's solution—only the reported confidence.
- IMPORTANT: You only learn whether the agent was correct in rounds where you DELEGATE.
  When you SELF_SOLVE, the agent's correctness remains unknown.

PAYOFF STRUCTURE:
- If you DELEGATE and the agent is correct: you pay {{ cost }} and earn reward {{ reward }} (net {{ reward - cost }}).
- If you DELEGATE and the agent is wrong: you pay {{ cost }} and earn nothing (net -{{ cost }}).
- If you SELF_SOLVE: you attempt the task yourself with a 50% chance of success worth {{ reward }} (expected gain {{ 0.5 * reward }}).

RATIONAL DECISION RULE:
- Delegate when (belief that the agent is correct) × {{ reward }} - {{ cost }} > {{ 0.5 * reward }}.
- Equivalently, DELEGATE if the agent's probability of being correct > {{ threshold }}.

{% if confidence_mode == "binary" %}
CONFIDENCE SCALE:
- LOW means the agent is not confident.
- HIGH means the agent is confident.
{% elif confidence_mode == "tercile" %}
CONFIDENCE SCALE:
- LOW / MEDIUM / HIGH map to increasing confidence levels.
{% endif %}

INTERACTION HISTORY:
{% if history %}
You have interacted with this agent before. For rounds where you DELEGATED, you observed correctness; otherwise, it remains unknown.

{% for h in history %}
Round {{ h.round }}:
{% if confidence_mode == "binary" %}
  - Reported confidence: {% if h.reported_confidence >= 0.5 %}HIGH{% else %}LOW{% endif %}
{% elif confidence_mode == "tercile" %}
  - Reported confidence: {% if h.reported_confidence >= 0.67 %}HIGH{% elif h.reported_confidence >= 0.33 %}MEDIUM{% else %}LOW{% endif %}
{% else %}
  - Reported confidence: {{ h.reported_confidence }}
{% endif %}
  - Your decision: {{ h.user_decision }}
{% if h.user_decision == "DELEGATE" %}
  - Agent correct: {{ h.was_correct }}
  - Your payoff: {{ h.user_payoff }}
{% else %}
  - Agent correctness: UNKNOWN (you self-solved)
  - Your payoff: {{ h.user_payoff }} (self-solving outcome)
{% endif %}
{% endfor %}

Summary:
- Total rounds: {{ history | length }}
- Delegations: {{ delegation_count }}
{% if delegation_count > 0 %}
- Agent accuracy when you delegated: {{ delegated_accuracy }}
- Delegations with high reported confidence: {{ high_confidence_delegation_count }}
{% if high_confidence_delegation_count > 0 %}
- Agent accuracy on high-confidence delegations: {{ high_confidence_delegated_accuracy }}
{% endif %}
{% endif %}
- Cumulative payoff: {{ total_payoff }}
{% else %}
This is your first interaction with the agent; you have no prior history.
{% endif %}

CURRENT ROUND INFORMATION:
{% if confidence_mode == "binary" %}
The agent reports {% if reported_confidence >= 0.5 %}HIGH{% else %}LOW{% endif %} confidence that its solution is correct.
{% elif confidence_mode == "tercile" %}
The agent reports {% if reported_confidence >= 0.75 %}HIGH{% elif reported_confidence >= 0.25 %}MEDIUM{% else %}LOW{% endif %} confidence that its solution is correct.
{% else %}
The agent reports a confidence of {{ reported_confidence }} that its solution is correct.
{% endif %}

YOUR DECISION:
Weigh the reported confidence against your threshold {{ threshold }}, consider the agent's historical calibration, and determine whether to DELEGATE or SELF_SOLVE.

Respond in this exact JSON format:
{
  "reasoning": "brief explanation of your decision",
  "decision": "DELEGATE" or "SELF_SOLVE",
  "belief_agent_correct": 0.X
}

Where "belief_agent_correct" is YOUR estimate (between 0.0 and 1.0) of the probability that the agent's solution is correct. This belief may differ from the reported confidence if you suspect miscalibration.
